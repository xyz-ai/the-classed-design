# the-classed-design
A project  can transt file  by udp , which made from python.一个项目，实现文件传输，通过udp通信，由python实现；如果你想，它可以简单地完成你的课设(使用时记得创建对应的txt文件)。
因为编码方式为“utf-8”,所以，并不支持其它格式的文件；但如果你想，那你就只需要修改一下其相应的编码方式，便能实现其它类型的文件的传输，例如：压缩包文件.但是，如果只是用来应付烦人的课设的话，那这个应该是足以用来交差的.
